- name: grant tomcat ownership on tomcat 
  command: sudo chown -R tomcat:tomcat /opt/tomcat/
     
- name: change to excute permission on user level on bin folder 
  command: sudo chmod -R u+x /opt/tomcat/bin